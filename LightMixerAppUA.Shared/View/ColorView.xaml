<Page
    x:Class="LightMixerAppUA.View.ColorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LightMixerAppUA.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock>Color</TextBlock>
        <Border BorderThickness="1" BorderBrush="Blue">
            <StackPanel Orientation="Vertical">
                <TextBlock>Red</TextBlock>
                <Slider Maximum="100" Minimum="1" Value="{Binding Red,Mode=TwoWay}" TickFrequency="5" SmallChange="5" LargeChange="25" />
                <TextBlock>Green</TextBlock>
                <Slider Maximum="100" Minimum="1" Value="{Binding Green,Mode=TwoWay}" TickFrequency="5" SmallChange="5" LargeChange="25" />
                <TextBlock>Blue</TextBlock>
                <Slider Maximum="100" Minimum="1" Value="{Binding Blue,Mode=TwoWay}" TickFrequency="5" SmallChange="5" LargeChange="25" />
                <CheckBox IsChecked="{Binding AutoChangeColor, Mode=TwoWay}">Auto Change Color</CheckBox>
                
            </StackPanel>
        </Border>
    </StackPanel>
</Page>
