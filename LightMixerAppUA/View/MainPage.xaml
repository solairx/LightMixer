<Page
    x:Class="LightMixerAppUA.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LightMixerAppUA"
    xmlns:view="using:LightMixerAppUA.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Hub>
            <HubSection Header="General">
                <DataTemplate>
                    <view:RgbLedView/>
                </DataTemplate>
            </HubSection>
            <HubSection Header="Led Detail">
                <DataTemplate>
                    <view:ColorView/>
                </DataTemplate>
            </HubSection>
            <HubSection Header="Moving Head">
                <DataTemplate>
                    <view:MovingHead/>
                </DataTemplate>
            </HubSection>

            <HubSection Header="Laser">
                <DataTemplate>
                    <view:LaserControler/>
                </DataTemplate>
            </HubSection>
        </Hub>
    </Grid>
</Page>
